import React from 'react';

const TopBar = () => (
    <header className="App-header">
        <p>
            <code className="App-title">docky</code>
        </p>
    </header>
);

export default TopBar;
